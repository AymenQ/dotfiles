- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig: gitconfig
    ~/.vimrc: vimrc
    ~/.vim: vim 
- shell:
  - [git submodule update --init --recursive, Installing submodules]
